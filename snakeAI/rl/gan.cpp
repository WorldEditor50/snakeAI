#include "gan.h"

RL::GAN::GAN(std::size_t generatorInputDim,
             std::size_t generatorOutputDim,
             std::size_t discriminatorInputDim,
             std::size_t discriminatorOutputDim)
{

}
